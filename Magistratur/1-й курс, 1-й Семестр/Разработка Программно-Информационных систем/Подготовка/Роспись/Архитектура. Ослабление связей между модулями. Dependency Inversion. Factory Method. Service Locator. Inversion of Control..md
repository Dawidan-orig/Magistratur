![[SOLID - D.png]]

![[Архитектура - Фабрика.png]]
![[Архитектура - Фабрика 1.png]]

![[Service Locator]]

#### Inversion of Control

_Модуль вообще не заботиться о «добывании» зависимостей._ Он лишь определяет, что ему нужно для работы, а все необходимые зависимости ему поставляются («впрыскиваются») из вне кем-то другим.  
  
Это так и называется — **Внедрение Зависимостей** (**Dependency Injection**). Обычно требуемые зависимости передаются либо в качестве параметров конструктора (_Constructor Injection_), либо через методы класса (_Setter injection_).  
  
Такой подход _инвертирует процесс создания зависимости_ — вместо самого модуля создание зависимостей контролирует кто-то извне. Модуль из активного элемента, становится пассивным — не он делает, а для него делают. Такое изменение направления действия называется **Инверсия Контроля** (**Inversion of Control**), или _Принцип Голливуда_ — «Не звоните нам, мы сами вам позвоним».