p - Это большое простое число. (Prime)
g - Обычное большое число (Generate)

Есть односторонняя функция $F(p,q)$, где $p,q$ -- это большие простые числа.

Функция эта выглядит как $Y(x) = D^x \mod P$, D < P.

